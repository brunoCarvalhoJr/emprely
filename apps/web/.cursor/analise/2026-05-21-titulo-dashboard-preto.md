# Analise - Titulo do dashboard em preto

## Componente afetado

`src/App.tsx`.

## Contexto tecnico

O titulo do hero do dashboard e renderizado no componente `DashboardContent`.

## Decisao

Substituir `text-primary` por `text-slate-950` apenas no titulo `Crie orcamentos profissionais em minutos`.
